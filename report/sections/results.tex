\section{Results}
\subsection{Results and Demo}
The primary quantitative results come from C-MAPSS FD001. The adaptive looped model produced the strongest macro F1 score on the held-out test set while using far fewer internal steps than the fixed-depth loop and far lower latency than both the frozen and task-adapted LLM baselines.

\subsection{Primary Findings}
\begin{itemize}
    \item The adaptive model achieved test macro F1 of 0.9183, outperforming the fixed-depth loop at 0.8891, the direct baseline at 0.8609, and the LLM baseline at 0.3183.
    \item The adaptive model required an average of only 1.21 recurrent steps on the test set, compared with 6.0 steps for the fixed-depth loop.
    \item Relative to the fixed-depth loop, the adaptive policy reduced average loop computation by approximately 79.8\%.
    \item The adaptive model averaged 0.17 ms/sample on CPU latency benchmarking, compared with 16.95 ms/sample for the LLM baseline, or roughly a 101x latency advantage.
    \item A stronger task-adapted DistilBERT baseline improved the LLM score to 0.3454 macro F1, but it still remained 0.5729 behind the adaptive loop and raised latency to 32.11 ms/sample.
    \item The adaptive model preserved strong critical-class performance, reaching perfect precision and 0.75 recall on the small critical test subset.
    \item Across the full local dataset matrix, the adaptive model outperformed the LLM baseline on every CMAPSS subset and all three IMS validation runs.
\end{itemize}

\subsection{All Dataset Results}
Table~\ref{tab:all-datasets} summarizes the canonical local benchmark matrix. C-MAPSS FD001 remains the full comparison point against the direct and fixed recurrent baselines, while the all-dataset matrix now includes adaptive and LLM rows for every local dataset. IMS results are reported on validation folds because those runs do not provide a separate official test split under the present experimental protocol.

\begin{table*}[t]
\centering
\small
\caption{Canonical local benchmark results}
\label{tab:all-datasets}
\begin{tabular}{@{}lllcc@{}}
\toprule
Dataset & Model & Split & Macro F1 & Latency (ms) \\
\midrule
CMAPSS FD001 & LLM & Test & 0.3183 & 16.95 \\
CMAPSS FD001 & Adaptive & Test & 0.9183 & 0.17 \\
CMAPSS FD002 & LLM & Test & 0.3163 & 29.58 \\
CMAPSS FD002 & Adaptive & Test & 0.9656 & n/a \\
CMAPSS FD003 & LLM & Test & 0.3239 & 33.02 \\
CMAPSS FD003 & Adaptive & Test & 0.8300 & n/a \\
CMAPSS FD004 & LLM & Test & 0.3219 & 16.76 \\
CMAPSS FD004 & Adaptive & Test & 0.9280 & n/a \\
IMS 1st\_test & LLM & Validation & 0.2481 & 29.43 \\
IMS 1st\_test & Adaptive & Validation & 0.9773 & n/a \\
IMS 2nd\_test & LLM & Validation & 0.2447 & 42.94 \\
IMS 2nd\_test & Adaptive & Validation & 0.9501 & n/a \\
IMS 4th\_test/txt & LLM & Validation & 0.2495 & 24.65 \\
IMS 4th\_test/txt & Adaptive & Validation & 0.9417 & n/a \\
\bottomrule
\end{tabular}
\end{table*}

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{figures/cmapss_comparison.png}
\caption{Primary C-MAPSS FD001 comparison. The adaptive loop achieves the best macro F1 score while using far less average loop depth than the fixed-depth recurrent model and clearly outperforming the text-serialized LLM baseline.}
\label{fig:cmapss-comparison}
\end{figure}

\subsection{Validation and Generalization}
On validation, the fixed-depth loop reached the best macro F1 score of 0.9366, while the adaptive model reached 0.9142 at an average depth of 1.39 steps. On the official test split, however, the adaptive model generalized better and delivered the strongest macro F1 score. The frozen LLM baseline, despite strong nominal accuracy from the dominant normal class, collapsed on macro F1 because it failed to separate warning and critical states reliably. The stronger task-adapted LLM narrowed that gap only slightly, rising from 0.3183 to 0.3454 macro F1 on the FD001 test set. This is the central comparison of the project: the looped numeric model handled minority degradation states far better than both generic and task-adapted text-transformer baselines while also avoiding the heavy latency cost of tokenization and transformer inference. The pattern suggests that fixed full-depth recurrence may fit the development split more aggressively, while adaptive early exit regularizes the model enough to improve test-time class balance without paying the cost of text-serialized inference.

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{figures/adaptive_history.png}
\caption{Adaptive model learning curve on C-MAPSS FD001. Validation macro F1 improves while average validation depth decreases over training, indicating that the model becomes both stronger and cheaper.}
\label{fig:adaptive-history}
\end{figure}

\begin{figure}[t]
\centering
\includegraphics[width=0.72\linewidth]{figures/adaptive_confusion.png}
\caption{Adaptive model confusion matrix on the C-MAPSS FD001 test set. Most residual errors occur between normal and warning states rather than catastrophic confusion across all classes.}
\label{fig:adaptive-confusion}
\end{figure}

\subsection{Secondary Dataset Status}
The IMS pipeline uses extracted run-to-failure vibration snapshots, summary-statistic feature generation, and cached preprocessing. Early temporal-split runs on IMS showed degenerate validation folds with missing classes, so the reported IMS results use validation-only evaluation. Under that setup, the adaptive model remained strong across the first, second, and fourth IMS runs, while the matched-window LLM baseline stayed clustered near macro F1 values of 0.24--0.25. The main quantitative claims remain anchored on C-MAPSS because it provides official train/test splits, but the full matrix shows that the adaptive-versus-LLM gap is not limited to a single FD001 slice.

\subsection{Demo Deliverable}
The completed system includes a Streamlit dashboard for live presentation use. The dashboard loads saved artifacts, displays benchmark comparisons, renders confusion matrices and training curves, and lets a user inspect individual telemetry windows with predicted health state, maintenance action, subsystem attribution, confidence, latency, and adaptive exit depth. That demo makes the quantitative comparison tangible by showing how the model behaves on specific examples instead of only aggregate tables.
